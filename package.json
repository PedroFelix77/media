{
  "name": "telemedicina-mediasoup-sfu",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "NODE_ENV=production node src/server.js",
    "build:client": "esbuild public/room.js --bundle --outfile=public/room.bundle.js --format=esm",
    "lint": "node -c src/server.js && node -c src/config.js && node -c src/logger.js && node -c src/mediasoup/workers.js && node -c src/rooms/rooms.js && node -c src/ws/wsServer.js && node -c src/http/httpServer.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "jsonwebtoken": "^9.0.2",
    "mediasoup": "^3.19.3",
    "mediasoup-client": "^3.18.7",
    "pino": "^9.9.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
